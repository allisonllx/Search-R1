# Search-R1 / veRL TRAINING environment — pip dependencies.
# Consumed automatically by `pip install -e .` (setup.py reads this file into
# install_requires; pyproject.toml [project.dependencies] mirrors the same list).
#
# torch is intentionally NOT listed here: install it first from the CUDA wheel index
# that matches the box, e.g.
#   pip install torch==2.8.0 --index-url https://download.pytorch.org/whl/cu128
#
# Pins reflect a known-good, mutually compatible set. The previous loose pins
# (transformers<4.48, vllm<=0.6.3) resolved on a fresh clone to transformers 4.47.1,
# which crashes against new torch with
#   ImportError: cannot import name 'Replicate' from 'torch.distributed.tensor'
# Keep transformers >=4.51 so its torch.distributed.tensor import is guarded.
#
# Do NOT add blank lines: setup.py does `item.strip()[0]` per line and an empty
# line raises IndexError. Full-line `#` comments are fine (they are filtered out).
accelerate==1.10.1
codetiming==1.4.0
datasets==3.6.0
dill==0.3.8
flash-attn==2.8.3
hydra-core==1.3.2
numpy>=2.0,<3
pandas==2.3.2
pybind11==3.0.1
ray==2.49.2
tensordict==0.9.1
tokenizers==0.22.1
transformers==4.56.2
vllm==0.10.2
wandb==0.23.1
IPython==9.5.0
matplotlib==3.10.6
