# Retriever environment — PIP packages (install after the conda step).
#
#   conda activate retriever
#   pip install -r requirements_retriever_pip.txt
#
# transformers is pinned >=4.51 for the same reason as the training env: transformers<4.48
# crashes against newer torch with
#   ImportError: cannot import name 'Replicate' from 'torch.distributed.tensor'
transformers==4.56.2
datasets==3.6.0
uvicorn==0.37.0
fastapi==0.135.1
# pyserini is only needed for the sparse/BM25 retriever; it also pulls in a JVM/Java deps.
# Left unpinned — drop this line if you only use the dense (e5) retriever.
pyserini
# faiss-cpu fallback for machines where faiss-gpu is unavailable or broken (e.g. H200).
# Install this INSTEAD of faiss-gpu from requirements_retriever_conda.txt on such boxes.
faiss-cpu==1.12.0
